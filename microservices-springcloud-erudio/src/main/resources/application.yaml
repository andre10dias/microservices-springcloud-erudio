spring:
  application:
    name: microservices-springcloud-erudio

  datasource:
    url: jdbc:postgresql://localhost:5432/springdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
